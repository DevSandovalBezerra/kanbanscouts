# Update — 21/03/2026

## Resumo

Implementação completa do sistema de **CRUD de Usuários** e **Controle de Permissões por Projeto**, conforme planejado em `docs/plano_usuarios_permissoes.md` (v2.0). Todas as medidas de segurança S01–S12 foram aplicadas.

---

## Novos Arquivos

### Controllers
- `app/Controllers/UserController.php` — CRUD de usuários (somente admin)
- `app/Controllers/ProjectMemberController.php` — Gerenciamento de membros por projeto

### Services
- `app/Services/User/CreateUserService.php`
- `app/Services/User/UpdateUserService.php`
- `app/Services/User/ToggleUserStatusService.php`
- `app/Services/User/ResetPasswordService.php`
- `app/Services/ProjectMember/AddMemberService.php`
- `app/Services/ProjectMember/UpdateMemberRoleService.php`
- `app/Services/ProjectMember/RemoveMemberService.php`

### Repositories
- `app/Repositories/PdoProjectMemberRepository.php`

### DTOs
- `app/DTO/UserDTO.php`
- `app/DTO/ProjectMemberDTO.php`

### Middleware & Políticas
- `app/Middleware/AdminMiddleware.php` — revalida `is_admin` diretamente no banco em cada request (nunca usa cache de sessão)
- `app/Policies/ProjectPolicy.php` — verifica papel do usuário no projeto (`canView`, `canWrite`, `canManageBoard`, `canInvite`, `canAlterRoles`, `canManageProject`)

### Validators
- `app/Validators/CreateUserValidator.php`
- `app/Validators/UpdateUserValidator.php`
- `app/Validators/InviteMemberValidator.php`

### Templates
- `templates/default/pages/admin-users.php` — painel de gerenciamento de usuários
- `templates/default/pages/project-members.php` — painel de membros do projeto

### Database
- `database/migrations/mysql/009_users_admin_project_roles.sql`
  - `ALTER TABLE users ADD COLUMN is_admin TINYINT(1) NOT NULL DEFAULT 0`
  - `ALTER TABLE project_members ADD COLUMN invited_by`, `accepted_at`
  - `CREATE TABLE admin_audit_log` — audit trail de ações administrativas

### Documentação
- `docs/plano_usuarios_permissoes.md` — plano de implementação v2.0
- `docs/security-report.md` — relatório de segurança do sistema

---

## Arquivos Modificados

### Controllers
- `app/Controllers/BoardController.php` — duplo guard: tenant isolation + `ProjectPolicy::canManageBoard()`
- `app/Controllers/ColumnController.php` — duplo guard + método `resolveProjectId()` via `ColumnRepository`
- `app/Controllers/ProjectController.php` — ao criar projeto, insere criador em `project_members` como `owner`
- `app/Controllers/TaskController.php` — duplo guard: tenant isolation + `ProjectPolicy::canWrite()`; injeção opcional de `PdoColumnRepository`, `PdoProjectRepository` e `ProjectPolicy`

### Repositories
- `app/Repositories/PdoBoardRepository.php` — método `belongsToCompany()`
- `app/Repositories/PdoColumnRepository.php` — método `resolveProjectId(int $columnId): ?int`
- `app/Repositories/PdoProjectRepository.php` — método `belongsToCompany(int $projectId, int $companyId): bool`
- `app/Repositories/PdoTaskRepository.php` — método `resolveProjectId(int $taskId): ?int`; `findByColumnId()` com `LEFT JOIN users` para `assignee_name` gracioso (exibe `null` quando usuário foi soft-deletado)

### Services & Auth
- `app/Services/AuthService.php` — grava `user_is_admin` na sessão após login (uso exclusivo de UI)
- `app/Services/PdoUserRepository.php` — `findActiveByEmail()` inclui `is_admin` no SELECT
- `app/Services/UserAuthRecord.php` — adicionado campo `isAdmin`

### DTOs
- `app/DTO/TaskDTO.php` — ajustes para compatibilidade com novos guards

### Rotas
- `routes/api.php` — novos endpoints:
  - `GET/POST/PATCH/DELETE /api/admin/users` (protegidos por `AdminMiddleware`)
  - `POST /api/admin/users/toggle-status`
  - `POST /api/admin/users/reset-password`
  - `GET/POST/PATCH/DELETE /api/project-members`
- `routes/web.php` — novas páginas:
  - `GET /admin/users` — painel admin (requer `is_admin`)
  - `GET /projects/{id}/members` — gerenciar membros

### Templates
- `templates/default/pages/projects.php` — ajustes visuais
- `templates/default/partials/sidebar.php` — exibe menu "Usuários" condicionalmente para admins (`user_is_admin` na sessão)

---

## Modelo de Papéis

### Global (`users.is_admin`)
| Valor | Descrição |
|-------|-----------|
| `true` | Administrador — CRUD completo de usuários da empresa |
| `false` | Membro comum |

### Por Projeto (`project_members.role_in_project`)
| Papel | Tarefas (escrita) | Boards/Colunas | Convidar | Alterar papéis | Editar projeto | Excluir projeto |
|-------|:-:|:-:|:-:|:-:|:-:|:-:|
| `owner` | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| `manager` | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ |
| `editor` | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| `viewer` | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |

---

## Segurança Aplicada

| Medida | Descrição |
|--------|-----------|
| S01 | `resolveProjectId()` nos repositories — previne spoofing de `project_id` no request |
| S02 | Endpoint PATCH em `/api/admin/users?id=X` com validação de campos |
| S03 | `AdminMiddleware` revalida `is_admin` no banco — revogação de privilégios tem efeito imediato |
| S04 | Endpoint dedicado `reset-password` separado do UPDATE de usuário |
| S05 | Bloqueio de soft-delete de `owner` com projetos ativos; retorna lista dos projetos afetados |
| S06 | Política de senha: mínimo 8 chars, 1 maiúscula, 1 número (`/^(?=.*[A-Z])(?=.*\d).{8,}$/`) |
| S07 | `admin_audit_log` — toda ação administrativa gera registro auditável |
| S08 | Duplo guard sequencial nos controllers: (1) tenant isolation → 404; (2) papel → 403 |
| S09 | `AddMemberService` valida que o `user_id` pertence à mesma empresa da sessão |
| S10 | `LEFT JOIN users` no `findByColumnId()` — tarefas com responsável deletado retornam `null` sem erro |
| S11 | Bloqueio de remoção/desativação do último admin da empresa |
| S12 | Somente `owner` pode alterar o papel de membros existentes (PATCH) |

---

## Novos Endpoints da API

### Usuários (requer AdminMiddleware)
| Método | Rota | Ação |
|--------|------|------|
| GET | `/api/admin/users` | Lista usuários da empresa |
| POST | `/api/admin/users` | Cria novo usuário |
| PATCH | `/api/admin/users?id=X` | Atualiza nome, e-mail, is_admin |
| POST | `/api/admin/users/toggle-status` | Ativa ou desativa usuário |
| POST | `/api/admin/users/reset-password` | Redefine senha |
| DELETE | `/api/admin/users?id=X` | Soft-delete de usuário |

### Membros do Projeto
| Método | Rota | Permissão |
|--------|------|-----------|
| GET | `/api/project-members?project_id=X` | Qualquer membro |
| POST | `/api/project-members` | `owner` ou `manager` |
| PATCH | `/api/project-members?id=X` | `owner` apenas |
| DELETE | `/api/project-members?id=X` | `owner` ou próprio usuário |

---

## Testes — Sprint 4 (21/03/2026)

### Novos arquivos de teste

#### Unit (`tests/Unit/`)
- `tests/Unit/AdminMiddlewareTest.php` — revalidação no banco, bloqueio de sessão stale (S03)
- `tests/Unit/ProjectPolicyTest.php` — hierarquia completa owner/manager/editor/viewer (S12)
- `tests/Unit/Validators/CreateUserValidatorTest.php` — política de senha (S06)
- `tests/Unit/Validators/UpdateUserValidatorTest.php` — campos opcionais, senha fraca
- `tests/Unit/Validators/InviteMemberValidatorTest.php` — roles válidos limitados a editor/viewer
- `tests/Unit/Services/ProjectMember/UpdateMemberRoleServiceTest.php` — guard de auto-rebaixamento do owner
- `tests/Unit/Services/ProjectMember/RemoveMemberServiceTest.php` — guard de auto-remoção do owner

#### Integration (`tests/Integration/`)
- `tests/Integration/AdminIntegrationTestCase.php` — base com seed de company/users/project
- `tests/Integration/CreateUserServiceTest.php` — hash de senha, lowercase, audit log (S07)
- `tests/Integration/UpdateUserServiceTest.php` — atualização parcial, audit log com before/after (S07)
- `tests/Integration/ToggleUserStatusServiceTest.php` — último admin protegido (S11)
- `tests/Integration/ResetPasswordServiceTest.php` — política de senha + isolamento de empresa
- `tests/Integration/AddMemberServiceTest.php` — cross-company rejeitado, duplicata bloqueada (S09)
- `tests/Integration/PdoProjectMemberRepositoryTest.php` — todos os métodos CRUD do repositório

### Refatoração de produção
- Criada interface `app/Repositories/ProjectMemberRepository.php`
- `PdoProjectMemberRepository`, `ProjectPolicy`, `UpdateMemberRoleService` e `RemoveMemberService` passam a depender da interface

### Resultado
| Suite | Testes | Asserções | Status |
|-------|--------|-----------|--------|
| Unit | 120 | 196 | ✅ |
| Integration | 71 | 149 | ✅ |
| Functional | 6 | 18 | ✅ |
| **Total** | **197** | **363** | **✅ 0 falhas** |

---

## Dados iniciais no banco (21/03/2026)

| Campo | Valor |
|-------|-------|
| **Empresa** | Test Corp (id=1, status=active) |
| **Admin** | sandoval@sbsystems.com.br / `password` — is_admin=1 |
| **Membro** | regular@test.com / `User@123` — is_admin=0 |

---

## Features — Sprint 5 (21/03/2026)

### Modal de Perfil do Usuário

Novo menu dropdown no avatar da navbar com modal de configuração de perfil self-service.

**Novos arquivos:**
- `app/Controllers/ProfileController.php` — 3 endpoints: update, password, avatar
- `database/migrations/mysql/010_users_avatar.sql` — coluna `avatar VARCHAR(255)` na tabela `users`
- `public/uploads/avatars/` — diretório de upload de avatares

**Arquivos modificados:**
- `routes/api.php` — rotas `POST /api/profile/update`, `/api/profile/password`, `/api/profile/avatar`
- `routes/web.php` — `sharedData` inclui `user_avatar`
- `templates/default/partials/navbar.php` — dropdown clicável no avatar com nome, email, links admin e logout
- `templates/default/layouts/base.html.php` — modal de perfil com abas "Perfil" e "Senha" + JS completo

**Funcionalidades:**
- Upload de avatar (JPG/PNG/WebP, máx 2MB) com preview imediato e fallback para iniciais
- Edição de nome e email (email exige senha atual para confirmar)
- Troca de senha (senha atual + nova + confirmar, validação em tempo real)
- Link "Gerenciar Usuários" no dropdown visível apenas para administradores

---

### Edição inline de cards no Kanban

Campos do card agora editáveis diretamente no modal de detalhe, sem necessidade de recriar a tarefa.

**Arquivos modificados:**
- `templates/default/pages/kanban.php` — título com edição inline (hover → lápis → input), prioridade como `<select>`, deadline como `<input type="date">` com botão de limpar
- `public/assets/js/kanban.js` — funções `startTitleEdit`, `saveTitle`, `cancelTitleEdit`, `handleTitleKey`, `updatePriority`, `updateDeadline`, `clearDeadline`; `openModal` atualizado para popular os novos campos

**Funcionalidades:**
- **Título**: hover no título exibe ícone de lápis; clique abre input inline; Enter salva, Esc cancela; card na coluna atualiza sem recarregar o board
- **Prioridade**: select dropdown substitui badge estático; mudança salva imediatamente e reflete no badge do header e no card
- **Prazo**: date picker inline com botão `×` para remover; atualiza o card sem recarregar
- **Responsável**: já existia; verificado e funcional (dropdown com todos os membros da empresa)

---

### Fix — Botões de ação em Membros do Projeto

**Arquivo modificado:**
- `templates/default/pages/project-members.php`

**Bug:** `/api/auth/me` retorna `{user: {id}}` mas o código lia `me.id` (undefined) em vez de `me.user.id`, fazendo `myRole` ficar sempre `null` — botões "Alterar Papel", "Remover" e "Adicionar Membro" nunca apareciam.

**Fix:** `me.id` → `me?.user?.id` com fallback seguro.

**Comportamento correto após fix:**
- Owner vê botões de ação em todos os membros
- Owner pode adicionar membros da empresa ao projeto com papel Editor ou Viewer
- Owner pode alterar o papel de qualquer membro (Owner / Manager / Editor / Viewer)
- Owner pode remover membros do projeto

---

## Hotfixes — Kanban/Deploy (21/03/2026)

### Quadro por projeto (Ir para o quadro)
- Ajustada a rota `GET /boards` para aceitar `id` como **board_id** ou **project_id**:
  - Quando `id` não for um board existente, tenta resolver como `project_id`.
  - Se o projeto existir e não houver board, cria automaticamente um board e colunas padrão (A Fazer, Em Progresso, Revisão, Concluído) e redireciona.

### Cache-busting de assets
- Adicionado versionamento por `filemtime()` para `assets/css/kanban.css` e `assets/js/kanban.js` na página do quadro, evitando cache de navegador/CDN após deploy.

### Exibição de descrição no card (tags HTML)
- Ajustado o render do card no `kanban.js` para exibir a descrição como texto (strip/normalização), evitando aparecerem tags como `<p><br></p>` no preview do card.
- Tratamento adicional para casos em que a descrição venha com HTML escapado.

### Calendar: URL base + CSRF + feedback
- Removido uso de `$appUrl` na view do calendário, padronizando com `BASE` (`app_url`).
- POST de criação de evento passa a enviar `X-CSRF-Token` e usa SweetAlert2 para sucesso/erro.

### Diagnóstico de variáveis de banco em hospedagem
- Bootstrap passa a registrar no log um evento `env_missing` quando variáveis de DB estiverem ausentes/vazias, sem expor credenciais, para facilitar diagnóstico de erros do tipo:
  - `Access denied for user 'root'@'localhost' (using password: NO)`

### Testes
- PHPUnit executado (Unit + suíte completa). Status: ✅ (com deprecations/skips reportados pelo PHPUnit).
